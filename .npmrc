; This project pulls one dependency straight from git (the "root" package,
; JannisX11/vue-prism-editor, per package.json) instead of the npm registry.
; npm blocks git-sourced deps by default (supply-chain hardening); this repo
; needs it, so allow it here rather than weakening the setting globally.
allow-git=all
